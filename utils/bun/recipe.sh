# bun -- JavaScript runtime, bundler and package manager
#
# Refer, not delegate. bun's installer appends PATH lines to ~/.zshrc,
# ~/.bashrc or fish's config whenever the file is writable, with no
# option to stop it. unflab doesn't edit shell config, and won't run
# something that does on the user's behalf -- so it says how, and lets
# them choose.

UNFLAB_NAME=bun
UNFLAB_KIND=refer
UNFLAB_HOMEPAGE=https://bun.sh/
